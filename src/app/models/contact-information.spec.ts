import { ContactInformation } from './contact-information';

describe('ContactInformation', () => {
  it('should create an instance', () => {
    expect(new ContactInformation()).toBeTruthy();
  });
});
